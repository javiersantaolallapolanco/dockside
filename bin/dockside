#!/bin/sh
set -eu

SELF_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
DOCKSIDE_ROOT="$(CDPATH= cd -- "$SELF_DIR/.." && pwd)"
export DOCKSIDE_ROOT

. "$DOCKSIDE_ROOT/core/bootstrap/bootstrap.sh"
. "$DOCKSIDE_ROOT/core/command/command.sh"

bootstrap_init

CMD="${1:-help}"

case "$CMD" in
    help|-h|--help)
        cat <<HELP
Dockside

Commands

  init
  doctor
  check
  status
  version
HELP
        ;;
    version)
        cat "$DOCKSIDE_ROOT/VERSION"
        ;;
    *)
        command_run "$CMD" "${@:2}"
        ;;
esac
